# CodingSummary Environment Variables
# Copy this file to .env and fill in your credentials

# Supabase PostgreSQL connection string (required)
# For local development: supabase start, then use the connection string below
# For remote Supabase: find it at https://supabase.com/dashboard/project/_/settings/database
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres

# Jira API Configuration
# Your Jira email address
JIRA_EMAIL=you@company.com

# Your Jira API token
# Generate one at: https://id.atlassian.com/manage-profile/security/api-tokens
JIRA_API_TOKEN=your-jira-api-token

# Bitbucket API Configuration (optional — falls back to JIRA_EMAIL + JIRA_API_TOKEN)
# Only needed if your Bitbucket account uses different credentials than Jira.
# BITBUCKET_EMAIL=you@company.com
# BITBUCKET_API_TOKEN=your-bitbucket-api-token
# Generate a token at: https://bitbucket.org/account/settings/api-tokens/
# Required scopes: repository:read, pullrequest:read
